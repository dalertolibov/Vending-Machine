package com.techelevator.data;

public class VendingMachine {

    private Inventory vendingMachineInventory;
















/*
    public static void main(String[] args) {

        Scanner keyboard = new Scanner(System.in);

        Menu ui = new Menu(keyboard);


        //Returns Users Input From Main Menu Prompt
        // Can ONLY accept "1"   "2"   "3"
        // Add more to code to defend against dumb user
        String mainMenuDisplay = ui.promptMainMenuDisplay();


        //IF user enters "1"
        // return <LIST> Item, Quantity, Slot, Price
        // displays "SOLD OUT" IF quantity = 0
        String displayVendingItems = ui.promptDisplayVendingMachineItems();

        //IF user enters "2"    entering 3 basically does nothing at main menu but is included
        //returns Purchasing Process Menu Prompt
        // Feed Money     Select Product     Finish Transaction
        String purchasingProcessMenu = ui.promptPurchasingProcessMenu();


        String feedMoney = ui.promptFeedMoney();


 */
        /*
        if (mainMenuDisplay.equals("1")) {
            return displayVendingItems;
        } else if (mainMenuDisplay.equals("2")) {
            return purchasingProcessMenu;
        } else if (mainMenuDisplay.equals("3")) {
            return mainMenuDisplay;
        } else {
            return "";
        }

        if (purchasingProcessMenu.equals("1")) {
            return feedMoney;
        } else if (purchasingProcessMenu.equals("2")) {
            return selectProduct;
        } else if (purchasingProcessMenu.equals("3")) {
            return finishTransaction;
        } else {
            return "";
        }
    }

         */

}


